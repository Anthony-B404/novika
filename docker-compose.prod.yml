name: novika-prod

services:
  # ===========================================
  # PostgreSQL Database
  # ===========================================
  postgres:
    image: postgres:16-alpine
    container_name: novika-postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_DATABASE}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - novika-internal

  # ===========================================
  # Redis (BullMQ)
  # ===========================================
  redis:
    image: redis:7-alpine
    container_name: novika-redis
    restart: always
    command: redis-server --appendonly yes ${REDIS_PASSWORD:+--requirepass $REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - novika-internal

  # ===========================================
  # Backend API (Production)
  # ===========================================
  backend:
    image: ghcr.io/${GITHUB_REPOSITORY:-anthonybette/novika}/backend:${TAG:-latest}
    container_name: novika-backend
    restart: always
    environment:
      NODE_ENV: production
      PORT: 3333
      HOST: 0.0.0.0
      APP_KEY: ${APP_KEY}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      # Database
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_DATABASE: ${DB_DATABASE}
      # Redis
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      # External APIs
      RESEND_API_KEY: ${RESEND_API_KEY}
      MISTRAL_API_KEY: ${MISTRAL_API_KEY}
      INWORLD_API_KEY: ${INWORLD_API_KEY}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      GOOGLE_CALLBACK_URL: ${GOOGLE_CALLBACK_URL}
      # URLs
      API_URL: https://api.novika.com
      FRONTEND_URL: https://novika.com
      # CORS
      ALLOWED_ORIGINS: https://novika.com
      # Drive
      DRIVE_DISK: local
    volumes:
      - backend_storage:/app/storage
    expose:
      - "3333"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://localhost:3333/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))",
        ]
      interval: 30s
      timeout: 10s
      start_period: 40s
      retries: 3
    networks:
      - novika-internal
      - novika-public
      - traefik-public
    labels:
      - "traefik.enable=true"
      # API principal
      - "traefik.http.routers.novika-api.rule=Host(`api.novika.com`)"
      - "traefik.http.routers.novika-api.entrypoints=websecure"
      - "traefik.http.routers.novika-api.tls.certresolver=mytlschallenge"
      - "traefik.http.services.novika-api.loadbalancer.server.port=3333"
      # Redirect api.cloud → api.com (301)
      - "traefik.http.routers.novika-api-cloud-redirect.rule=Host(`api.novika.cloud`)"
      - "traefik.http.routers.novika-api-cloud-redirect.entrypoints=websecure"
      - "traefik.http.routers.novika-api-cloud-redirect.tls.certresolver=mytlschallenge"
      - "traefik.http.routers.novika-api-cloud-redirect.middlewares=redirect-api-cloud-to-com"
      - "traefik.http.middlewares.redirect-api-cloud-to-com.redirectregex.regex=^https://api\\.novika\\.cloud(.*)"
      - "traefik.http.middlewares.redirect-api-cloud-to-com.redirectregex.replacement=https://api.novika.com$${1}"
      - "traefik.http.middlewares.redirect-api-cloud-to-com.redirectregex.permanent=true"
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M

  # ===========================================
  # Backend Worker (Production)
  # ===========================================
  worker:
    image: ghcr.io/${GITHUB_REPOSITORY:-anthonybette/novika}/backend:${TAG:-latest}
    container_name: novika-worker
    restart: always
    command: ["node", "bin/worker.js"]
    environment:
      NODE_ENV: production
      PORT: 3333
      HOST: 0.0.0.0
      APP_KEY: ${APP_KEY}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      # Database
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_DATABASE: ${DB_DATABASE}
      # Redis
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      # External APIs
      RESEND_API_KEY: ${RESEND_API_KEY}
      MISTRAL_API_KEY: ${MISTRAL_API_KEY}
      INWORLD_API_KEY: ${INWORLD_API_KEY}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      GOOGLE_CALLBACK_URL: ${GOOGLE_CALLBACK_URL}
      # URLs
      API_URL: https://api.novika.com
      FRONTEND_URL: https://novika.com
      # Drive
      DRIVE_DISK: local
    volumes:
      - backend_storage:/app/storage
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - novika-internal
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G

  # ===========================================
  # Scheduler (Cron Jobs)
  # ===========================================
  scheduler:
    build:
      context: ./docker/scheduler
      args:
        BACKEND_IMAGE: ghcr.io/${GITHUB_REPOSITORY:-anthonybette/novika}/backend:${TAG:-latest}
    container_name: novika-scheduler
    restart: always
    environment:
      NODE_ENV: production
      APP_KEY: ${APP_KEY}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      # Database
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_DATABASE: ${DB_DATABASE}
      # Redis
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      # External APIs
      RESEND_API_KEY: ${RESEND_API_KEY}
      MISTRAL_API_KEY: ${MISTRAL_API_KEY}
      INWORLD_API_KEY: ${INWORLD_API_KEY}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      GOOGLE_CALLBACK_URL: ${GOOGLE_CALLBACK_URL}
      # URLs
      API_URL: https://api.novika.com
      FRONTEND_URL: https://novika.com
      # Drive
      DRIVE_DISK: local
    volumes:
      - backend_storage:/app/storage
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - novika-internal
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  # ===========================================
  # Frontend (Production)
  # ===========================================
  frontend:
    image: ghcr.io/${GITHUB_REPOSITORY:-anthonybette/novika}/frontend:${TAG:-latest}
    container_name: novika-frontend
    restart: always
    expose:
      - "80"
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "http://127.0.0.1:80/",
        ]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3
    networks:
      - novika-public
      - traefik-public
    labels:
      - "traefik.enable=true"
      # Frontend principal
      - "traefik.http.routers.novika-frontend.rule=Host(`novika.com`) || Host(`www.novika.com`)"
      - "traefik.http.routers.novika-frontend.entrypoints=websecure"
      - "traefik.http.routers.novika-frontend.tls.certresolver=mytlschallenge"
      - "traefik.http.services.novika-frontend.loadbalancer.server.port=80"
      # Redirect .cloud → .com (301)
      - "traefik.http.routers.novika-cloud-redirect.rule=Host(`novika.cloud`) || Host(`www.novika.cloud`)"
      - "traefik.http.routers.novika-cloud-redirect.entrypoints=websecure"
      - "traefik.http.routers.novika-cloud-redirect.tls.certresolver=mytlschallenge"
      - "traefik.http.routers.novika-cloud-redirect.middlewares=redirect-cloud-to-com"
      - "traefik.http.middlewares.redirect-cloud-to-com.redirectregex.regex=^https://(?:www\\.)?novika\\.cloud(.*)"
      - "traefik.http.middlewares.redirect-cloud-to-com.redirectregex.replacement=https://novika.com$${1}"
      - "traefik.http.middlewares.redirect-cloud-to-com.redirectregex.permanent=true"
      # Redirect .fr → .com (301)
      - "traefik.http.routers.novika-fr-redirect.rule=Host(`novika.fr`) || Host(`www.novika.fr`)"
      - "traefik.http.routers.novika-fr-redirect.entrypoints=websecure"
      - "traefik.http.routers.novika-fr-redirect.tls.certresolver=mytlschallenge"
      - "traefik.http.routers.novika-fr-redirect.middlewares=redirect-fr-to-com"
      - "traefik.http.middlewares.redirect-fr-to-com.redirectregex.regex=^https://(?:www\\.)?novika\\.fr(.*)"
      - "traefik.http.middlewares.redirect-fr-to-com.redirectregex.replacement=https://novika.com$${1}"
      - "traefik.http.middlewares.redirect-fr-to-com.redirectregex.permanent=true"
    deploy:
      resources:
        limits:
          memory: 128M
        reservations:
          memory: 64M

volumes:
  postgres_data:
  redis_data:
  backend_storage:

networks:
  novika-internal:
    driver: bridge
    # Removed 'internal: true' - was blocking outbound connections for OAuth
  novika-public:
    driver: bridge
  traefik-public:
    external: true
    name: traefik-public
