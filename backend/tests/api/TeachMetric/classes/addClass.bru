meta {
  name: Ajouter une classe
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/classes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Classe de test"
  }
}

tests {
  test("devrait créer une nouvelle classe", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(201);
    expect(data.message).to.equal('Classe ajoutée avec succès');
  });
}
