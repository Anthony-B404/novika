meta {
  name: Supprimer une classe
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/classes/1
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("devrait supprimer une classe", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(data.message).to.equal('Classe supprimée avec succès');
  });
}
