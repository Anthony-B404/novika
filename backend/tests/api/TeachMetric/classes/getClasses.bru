meta {
  name: Obtenir toutes les classes
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/classes
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("devrait retourner la liste des classes", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(data).to.be.an('array');
  });
}
