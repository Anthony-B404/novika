meta {
  name: Modifier un étudiant
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/students/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "firstName": "Jean",
    "lastName": "Martin",
    "email": "jean.martin@test.fr",
    "classId": 2
  }
}

tests {
  test("devrait modifier un étudiant", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(data.message).to.equal('Étudiant modifié avec succès');
  });
}
