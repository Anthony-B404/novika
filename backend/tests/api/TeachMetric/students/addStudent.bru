meta {
  name: Ajouter un étudiant
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/students
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "firstName": "Jean",
    "lastName": "Dupont",
    "email": "jean.dupont@test.fr",
    "classId": 2
  }
}

tests {
  test("devrait créer un nouvel étudiant", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(201);
    expect(data.message).to.equal('Étudiant ajouté avec succès');
  });
}
