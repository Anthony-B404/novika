


meta {
  name: Supprimer un étudiant
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/students/1
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("devrait supprimer un étudiant", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(data.message).to.equal('Étudiant supprimé avec succès');
  });
}