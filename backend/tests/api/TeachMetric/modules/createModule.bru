meta {
  name: Create Module
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/modules
  body: json
  auth: bearer
}

auth:bearer {   
  token: {{token}}
}

body:json {
  {
    "name": "Mathématiques",
  }
}

tests {
  test("response status is 201") {
    expect(response.status).toBe(201)
  }
  
  test("response has module data") {
    expect(response.body).toHaveProperty("module")
    expect(response.body.module).toHaveProperty("id")
    expect(response.body.module).toHaveProperty("name")

  }
} 