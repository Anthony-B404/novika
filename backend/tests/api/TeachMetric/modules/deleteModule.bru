meta {
  name: Delete Module
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/modules/1
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("response status is 200") {
    expect(response.status).toBe(200)
  }
  
  test("response has success message") {
    expect(response.body).toHaveProperty("message")
    expect(response.body.message).toBe("Module supprimé avec succès")
  }
} 