meta {
  name: Me
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/me
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{authToken}}
}

auth:bearer {
  token: {{token}}
}

tests {
  test("should get user info", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(data).to.have.property('id');
    expect(data.email).to.equal('test@example.com');
  });
}
